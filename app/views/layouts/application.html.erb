<!DOCTYPE html>
<html class="h-full">
  <head>
    <title><%= content_for(:title) || "Veritas - Digital Truth Archive" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="description" content="Archive and verify corporate terms of service with cryptographic guarantees. Ensure companies can't silently rewrite history.">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body class="h-full bg-gray-50">
    <!-- Navigation -->
    <nav class="bg-white shadow-sm border-b border-gray-200 fixed w-full top-0 z-50">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16">
          <div class="flex items-center">
            <%= link_to root_path, class: "flex items-center space-x-3" do %>
              <div class="w-8 h-8 bg-gradient-to-br from-blue-600 to-purple-600 rounded-lg flex items-center justify-center">
                <svg class="w-5 h-5 text-white" fill="currentColor" viewBox="0 0 20 20">
                  <path fill-rule="evenodd" d="M2.166 4.999A11.954 11.954 0 0010 1.944 11.954 11.954 0 0017.834 5c.11.65.166 1.32.166 2.001 0 5.225-3.34 9.67-8 11.317C5.34 16.67 2 12.225 2 7c0-.682.057-1.35.166-2.001zm11.541 3.708a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
                </svg>
              </div>
              <span class="text-xl font-bold text-gray-900">Veritas</span>
            <% end %>
          </div>
          
          <div class="flex items-center space-x-8">
            <%= link_to "Browse Companies", companies_path, class: "text-gray-600 hover:text-gray-900 font-medium transition-colors duration-200" %>
            <%= link_to "Recent Archives", documents_path, class: "text-gray-600 hover:text-gray-900 font-medium transition-colors duration-200" %>
            <%= link_to "Add Company", new_company_path, class: "bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-lg font-medium transition-colors duration-200" %>
          </div>
        </div>
      </div>
    </nav>

    <!-- Flash Messages -->
    <% flash.each do |type, message| %>
      <div class="fixed top-20 right-4 z-50 max-w-sm">
        <div class="bg-<%= type == 'notice' ? 'green' : 'red' %>-50 border border-<%= type == 'notice' ? 'green' : 'red' %>-200 rounded-lg p-4 shadow-lg">
          <div class="flex">
            <div class="flex-shrink-0">
              <% if type == 'notice' %>
                <svg class="h-5 w-5 text-green-400" viewBox="0 0 20 20" fill="currentColor">
                  <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
                </svg>
              <% else %>
                <svg class="h-5 w-5 text-red-400" viewBox="0 0 20 20" fill="currentColor">
                  <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd" />
                </svg>
              <% end %>
            </div>
            <div class="ml-3">
              <p class="text-sm text-<%= type == 'notice' ? 'green' : 'red' %>-800"><%= message %></p>
            </div>
          </div>
        </div>
      </div>
    <% end %>

    <!-- Main Content -->
    <main class="pt-16 min-h-screen">
      <%= yield %>
    </main>

    <!-- Footer -->
    <footer class="bg-gray-900 text-white mt-20">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
        <div class="grid grid-cols-1 md:grid-cols-4 gap-8">
          <div class="md:col-span-2">
            <div class="flex items-center space-x-3 mb-4">
              <div class="w-8 h-8 bg-gradient-to-br from-blue-600 to-purple-600 rounded-lg flex items-center justify-center">
                <svg class="w-5 h-5 text-white" fill="currentColor" viewBox="0 0 20 20">
                  <path fill-rule="evenodd" d="M2.166 4.999A11.954 11.954 0 0010 1.944 11.954 11.954 0 0017.834 5c.11.65.166 1.32.166 2.001 0 5.225-3.34 9.67-8 11.317C5.34 16.67 2 12.225 2 7c0-.682.057-1.35.166-2.001zm11.541 3.708a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
                </svg>
              </div>
              <span class="text-xl font-bold">Veritas</span>
            </div>
            <p class="text-gray-400 mb-4">
              Defending digital truth through transparency. An immutable archive of corporate terms and policies.
            </p>
            <p class="text-sm text-gray-500">
              Because the internet never forgets, but companies hope you do.
            </p>
          </div>
          <div>
            <h3 class="text-sm font-semibold text-gray-300 uppercase tracking-wider mb-4">Browse</h3>
            <ul class="space-y-2">
              <li><%= link_to "Companies", companies_path, class: "text-gray-400 hover:text-white transition-colors duration-200" %></li>
              <li><%= link_to "Recent Archives", documents_path, class: "text-gray-400 hover:text-white transition-colors duration-200" %></li>
            </ul>
          </div>
          <div>
            <h3 class="text-sm font-semibold text-gray-300 uppercase tracking-wider mb-4">Contribute</h3>
            <ul class="space-y-2">
              <li><%= link_to "Add Company", new_company_path, class: "text-gray-400 hover:text-white transition-colors duration-200" %></li>
              <li><a href="https://github.com/weirdindiankid/veritas" class="text-gray-400 hover:text-white transition-colors duration-200">GitHub</a></li>
            </ul>
          </div>
        </div>
        <div class="border-t border-gray-800 mt-8 pt-8 text-center">
          <p class="text-gray-400 text-sm">
            © <%= Date.current.year %> Veritas by Dharmesh Tarapore. Open source under GPLv2 License.
          </p>
        </div>
      </div>
    </footer>

    <%= javascript_importmap_tags %>
  </body>
</html>
